<div class="row">
    <!-- Nav tabs -->

    <!-- Tab panes -->

    <div class="container container-fluid">
        <div class="row">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <tr>
                        <th>Consistent ID</th>
                        <th>Order</th>
                        <th>Address</th>
                        <th>Host</th>
                        <th>ID</th>
                        <th>Type</th>
                        <th></th>
                    </tr>
                    {% for node in nodes %}
                        <tr>
                            <td><a href="/node/{{ node.id }}">{{ node.consistentId }}</a></td>
                            <td>{{ node.order }}</td>
                            <td>{{ node.addresses }}</td>
                            <td>{{ node.hostNames }}</td>
                            <td>{{ node.id }}</td>
                            <td>{% if node.isClient %}Client{% else %}Server{% endif %}</td>
                            <td><a class="btn btn-default" href="/node/{{ node.id }}"><i class="fa fa-info" aria-hidden="true"></i> Show</a></td>
                        </tr>
                    {% endfor %}
                </table>
            </div>
        </div>
    </div>
</div>